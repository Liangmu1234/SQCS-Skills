interface:
  display_name: "SSH Content"
  short_description: "One-step SSH bootstrap, diagnostics, scripts, and transfers"
  default_prompt: "Use $ssh-content for a secure one-step SSH connection, remote operation, or verified file transfer."
